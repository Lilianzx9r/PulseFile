# v1.4.2 — archive menu fix

The actual visible selection toolbar now contains an overflow menu with:

- Compresser vers…
- Décompresser vers… (only when exactly one ZIP is selected)

The local per-item context menu retains the same archive actions.

FTP and HTTP context menus also expose Compresser vers… for directories, not only files.
